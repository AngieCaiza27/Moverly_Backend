# Framework principal
fastapi==0.115.0
uvicorn[standard]==0.30.1

# Base de datos (SQLAlchemy async + Alembic)
SQLAlchemy==2.0.32
alembic==1.13.2
psycopg[binary,async]>=3.0

# Validación y utilidades
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# Seguridad (JWT tokens)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# CORS y Starlette
starlette==0.37.2

# Testing
pytest==8.3.2
httpx==0.27.0

# Compatibility fixes for Windows / async runtime
# pin bcrypt to a version supported by passlib 1.7.4
bcrypt==3.2.0
# greenlet is required by SQLAlchemy async utilities
greenlet>=2.0.2

# Framework principal
fastapi==0.115.0
uvicorn[standard]==0.30.1

# Manejo de base de datos (si usas SQLAlchemy + Alembic)
sqlalchemy==2.0.32
alembic==1.13.2

# Conexión a PostgreSQL (puedes cambiarlo según la BD que uses)
psycopg2-binary==2.9.9

# Validación y utilidades
pydantic==2.8.2
python-dotenv==1.0.1

# Seguridad (JWT tokens)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# CORS y seguridad extra
starlette==0.37.2

# Testing
pytest==8.3.2
httpx==0.27.0
